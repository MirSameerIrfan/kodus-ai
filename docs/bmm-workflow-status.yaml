# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "{{generated}}"
project: "kodus-ai"
# template-output checkpoint
user_description: |
  Novo ciclo foca em diagnosticar e melhorar o pipeline de code review do Kodus, que hoje
  executa uma sequência de estágios NestJS: valida commits/config, busca diffs, injeta
  contexto externo, gera comentário inicial, roda fine-tuning do Kody, faz análise AST,
  revisa em nível de PR e de arquivo, consolida resultados e decide aprovar ou pedir
  mudanças. Queremos mapear pontos fortes, gargalos e oportunidades de otimização.
# template-output checkpoint
field_type: brownfield
# template-output checkpoint
project_type: software
# template-output checkpoint
brainstorm_requested: true
# template-output checkpoint
research_requested: true
# template-output checkpoint
selected_track: method
project_type: "{{project_type}}"
selected_track: "{{selected_track}}"
field_type: "{{field_type}}"
workflow_path: "{{workflow_path_file}}"
workflow_status: "{{workflow_items}}"
